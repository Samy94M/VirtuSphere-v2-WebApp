# Gilt nur fuer Builds mit Repo-Root-Kontext (Docker/qa-ansible/Dockerfile).
# Die Anwendungs-Images bauen mit ./Docker/nginx bzw. ./Docker/php als Kontext
# und sehen diese Datei nicht. Alles ausschliessen, nur die zwei Dateien
# hereinlassen, die das QA-Image kopiert.
*
!Docker/qa-ansible/requirements.txt
!Ansible/requirements.yml
